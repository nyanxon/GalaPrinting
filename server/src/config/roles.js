/**
 * roles.js — Shared role constants.
 * Single source of truth for staff role lists used across routes,
 * controllers, services, and socket handlers.
 */

export const STAFF_ROLES = ['admin', 'owner', 'cashier', 'cs', 'operational', 'qc', 'offline'];
